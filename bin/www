#!/usr/bin/env node
"use strict";

//module dependencies
var server = require("../dist/server/server/server");
var debug = require("debug")("express:server");
var http = require("http");

//create http server
var appServer = server.Server.bootstrap();